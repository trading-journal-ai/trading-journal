# Design archive

These documents are **superseded**. They are kept only for history — do not
treat any of them as current guidance, and do not link to them from active docs.

The single source of truth is [`../DESIGN_SYSTEM.md`](../DESIGN_SYSTEM.md), with
token *values* owned by [`src/app/globals.css`](../../../src/app/globals.css).

| File | Why archived | Superseded by |
| --- | --- | --- |
| `DESIGN_SYSTEM_ONE_SHEET.md` | "Deep" single-theme checklist (Jun 2026). Its palette values, phantom tokens (`--page-bg`, `--prose`), and "Use With Codex" migration checklist no longer match the shipped 4-theme system. | `../DESIGN_SYSTEM.md` |
| `DESIGN_SYSTEM_ONE_SHEET.html` | Visual browser artifact for the same stale "Deep" palette. | Regenerate from `globals.css` if a visual sheet is wanted again. |
| `design-qa-ticker-review-v1.md` | One-off QA log for the ticker-review V1 build. References ephemeral `/tmp` and Desktop screenshots that no longer exist. | — (process log, not a spec) |
| `design-qa-journal-wireframe.md` | One-off QA log for the journal data-exploration wireframe. Same ephemeral references. | — (process log, not a spec) |
